flask==3.0.0
flask-cors==4.0.0
requests==2.31.0
Pillow==10.2.0
opencv-python-headless==4.9.0.80
numpy==1.26.4
imagehash==4.3.1
python-dotenv==1.0.0
psycopg2-binary==2.9.9
# Optional – enables barcode scanning on pack labels (requires libzbar0 system lib)
# Install: apt-get install libzbar0 && pip install pyzbar
pyzbar==0.1.9
# Optional – enables OCR card-text extraction (requires tesseract-ocr system package)
# Install: apt-get install tesseract-ocr && pip install pytesseract
pytesseract==0.3.10
